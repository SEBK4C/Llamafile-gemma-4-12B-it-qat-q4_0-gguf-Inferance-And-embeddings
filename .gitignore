# Model weights (fetched via `make model`, ~7.1 GB)
models/

# Built artifacts (built via `make build` / `make package`)
bin/
dist/

# Server runtime droppings
*.log
*.tmp
*.bak
*.swp
.cache/
__pycache__/
*.pyc
.DS_Store
.kvcache/
.gemma4-kv/
